let numbers = [100, 34, 5, 67, 72];
// En utilisant la récursion, écrivez le code permettant de trouver le min et le max de cette liste
let minimum = numbers.// ...
let maximum = numbers.// ...
console.log(`Min is ${minimum}, max is ${maximum}`);

//A retravailler
let numbers = [100, 34, 5, 67, 72];
// En utilisant la récursion, écrivez le code permettant de trouver le min et le max de cette liste

let minimum: (let min=null, let i=numbers.length
    while (i>0){
        i--;
        numbers[i]<?mini:mini=numbers[i]
    }
    return min;
}
console.log(`Min is ${minimum}`);
// ...
//let maximum = numbers.// ...

//console.log(`Min is ${minimum}, max is ${maximum}`);


let numbers = [100, 34, 5, 67, 72];
// En utilisant la récursion, écrivez le code permettant de trouver le min et le max de cette liste

let minimum= numbers => min => current => 

, let i=numbers.length
    while (i>0){
        i--;
        numbers[i]<mini?mini=numbers[i]:mini;
    }
    return min;
}
console.log(`Min is ${minimum}`);
// ...
//let maximum = numbers.// ...

//console.log(`Min is ${minimum}, max is ${maximum}`);